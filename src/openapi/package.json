{
  "scripts": {
    "build": "npm run schema && npm run typescript && npm run pre-commit && npm run typescript.replace",
    "schema": "cd ../python && poetry run assistance schema ../openapi/openapi.json",
    "typescript": "npm run typescript.clear && npm run typescript.generate",
    "typescript.clear": "rm -r ../typescript/assistance/src/client || exit 0",
    "typescript.generate": "cd ../typescript/assistance && npm run generate && npm run build",
    "typescript.replace": "replace-in-files --string=\"url: '/\" --replacement=\"url: 'https://api.assistance.chat/\" \"../typescript/assistance/src/client/**/*.{ts,tsx}\"",
    "pre-commit": "pre-commit run --all-files || exit 0"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.2",
    "replace-in-files-cli": "^2.0.0"
  }
}
