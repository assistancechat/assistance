{
  "name": "assistance",
  "version": "0.1.0",
  "description": "",
  "main": "",
  "scripts": {
    "stage": "mkdir dist || echo Dist already exists.",
    "bundle": "esbuild src/loader/gmail-js-loader.js src/main.ts --outdir=dist/ --bundle --sourcemap --target=es6",
    "build": "npm run stage && npm run bundle"
  },
  "author": "Simon Biggs",
  "license": "Apache-2.0",
  "dependencies": {
    "gmail-js": "^1.1.1",
    "jquery": "^3.6.1"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "esbuild": "^0.16.0",
    "typescript": "^4.9.5"
  }
}
