Simulates the Game of Life using a randomly initialised board.

Parameters
----------
board_size : tuple of int
    The size of the board as a tuple of (rows, columns).
    Default is (10, 10).
num_generations : int
    The number of generations to simulate. Default is 10.

Returns
-------
None
    Displays the board at each generation using IPython display calls.
