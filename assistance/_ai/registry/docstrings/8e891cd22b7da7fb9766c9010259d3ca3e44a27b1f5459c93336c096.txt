Initialises a board of the given size with random values.

Parameters
----------
board_size : tuple of int
    The size of the board as a tuple of (rows, columns).
    Default is (10, 10).

Returns
-------
list[list[int]]
    The randomly initialised board as a list of lists of integers.
